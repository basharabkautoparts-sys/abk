node_modules

# Output
/build
/.svelte-kit
/package

# Local env (Supabase credentials for `pnpm dev` / local builds)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log

# Tooling (machine-local)
.claude/settings.local.json

# Source artwork kept out of the repo; the exported assets live in static/
/*.jpg
/*.png
