/**
 * Credentials for demo mode — used only when Supabase is not configured, so the
 * admin can be explored locally. Real staff accounts live in Supabase Auth and
 * are authorised through the `staff` table.
 */
export const DEMO_ADMIN_EMAIL = 'admin@abkautoparts.local';
export const DEMO_ADMIN_PASSWORD = 'abk-demo-admin';
